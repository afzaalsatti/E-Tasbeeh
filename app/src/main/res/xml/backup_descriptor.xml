<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <exclude
        domain="database"
        path="ZIKARTABLE" />
    <exclude
        domain="database"
        path="tasbeeh.db" />
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="Test PREFS.xml" />
    <exclude
        domain="sharedpref"
        path="Prayer PREFS.xml" />
</full-backup-content>
